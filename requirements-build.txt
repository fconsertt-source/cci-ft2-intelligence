# ═══════════════════════════════════════════════════════════
# CCI-FT2-Intelligence — Build / Packaging Dependencies
# ═══════════════════════════════════════════════════════════
# تحديث: v2.2 — Security Hardening Phase
# ───────────────────────────────────────────────────────────
# الاستخدام (في بيئة build نظيفة فقط):
#   pip install -r requirements.txt -r requirements-build.txt
#   pyinstaller GuardianFT2.spec
# ───────────────────────────────────────────────────────────
# ⚠️  لا تثبّت هذا الملف في بيئة التطوير العادية
# ═══════════════════════════════════════════════════════════

# ── Base Production Dependencies ───────────────────────────
-r requirements.txt

# ── PyInstaller (Pinned for Reproducibility) ───────────────
pyinstaller==6.19.0
pyinstaller-hooks-contrib==2026.1
altgraph==0.17.5
pefile==2024.8.26
pywin32-ctypes==0.2.3
setuptools==82.0.0
filelock==3.24.3
packaging==26.0

# ── Build Security Checks ──────────────────────────────────
# Run before building: pip-audit && safety check
pip-audit==2.7.3
safety==3.2.0